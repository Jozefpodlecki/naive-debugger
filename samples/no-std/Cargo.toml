cargo-features = ["panic-immediate-abort"]

[package]
name = "no-std-examples"
version = "0.1.0"
edition = "2024"
rust-version = "1.97.0"

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
