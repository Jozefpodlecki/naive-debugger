[workspace]
resolver = "2"
members = [
    "debugger",
    "agent",
    # "samples/no-std",
    "samples/with-std"
]

default-members = ["debugger"]

# [profile.dev.package.no-std-examples]
# panic = "abort"

# [profile.release.no-std-examples]
# panic = "abort"